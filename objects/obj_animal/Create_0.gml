/// @description Creates the animal model and transformation matrix

buffer = pb_cube(6.0, 3.0, 3.0, c_purple);

m_world = matrix_build_identity();

requires_update = true;

// 3D model variables
z = 0;